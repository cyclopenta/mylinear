---
title: "tutorial"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{tutorial}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```
## Introdction  
This package integrates the basic elements in the linear regression based on BIOSTAT650. The main function *mylm* will return a list of 5 items, They are:  
*results*: dataframe, with similar format with summary(lm)  
*residuals*: The residuals of the model
*R.square*: The R^2 which demonstrate the percentage of variance in Y explained by the model
  out_list =  list(results = results, residuals = res, R.square = R_square,
                   R.square.adj = R_adj_square, SSE = SSE)

```{r setup}
library(qwzLinear)

```
## Testhats
```{r}
#library(testthat)
#test_check("qwzLinear", path = 'D:/zl/Umich/course/biostat625/qwzLinear/tests/')
```

##Basic Tests
```{r}
```

##Comparsion
```{r}
```
